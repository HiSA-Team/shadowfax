# Experiments

This directory contains scripts, metadata, and raw results intended to make
Shadowfax measurements reproducible. Unlike generated binaries in `bin/` and
`target/`, completed run directories are source-controlled research data.

Each experiment documents its own protocol and result schema. Do not edit a
completed run in place; make a new run when the software, toolchain, machine,
or protocol changes.
